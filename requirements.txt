# Minimal deployment requirements (production only)
# Heavy packages like torch, transformers, langchain removed for deployment

# Core ML (only what's needed)
sentence-transformers>=2.2.0

# Vector Database
faiss-cpu>=1.7.4

# Document Processing
pypdf>=3.0.0
python-docx>=0.8.11
beautifulsoup4>=4.12.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Email System
Flask>=3.0.0
Flask-SQLAlchemy>=3.0.0
Flask-CORS>=4.0.0
psycopg2-binary>=2.9.0
MSAL>=1.24.0
requests>=2.31.0
langdetect>=1.0.9
gunicorn>=21.2.0

# React Frontend (served by Flask)
# Install Node.js separately for React development
